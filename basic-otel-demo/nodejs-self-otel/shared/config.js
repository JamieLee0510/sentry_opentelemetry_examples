const SERVER_A_PORT = 3051;
const SERVER_A_URL = `http://localhost:${SERVER_A_PORT}`;
const SERVER_B_PORT = 3052;
const SERVER_B_URL = `http://localhost:${SERVER_B_PORT}`;
const SELF_COLLECTOR_PORT = 3050;
const SELF_COLLECTOR_URL = `http://localhost:${SELF_COLLECTOR_PORT}`;

module.exports = {
    SERVER_A_PORT,
    SERVER_A_URL,
    SERVER_B_PORT,
    SERVER_B_URL,
    SELF_COLLECTOR_PORT,
    SELF_COLLECTOR_URL,
};
